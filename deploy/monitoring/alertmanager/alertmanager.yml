global:
  resolve_timeout: 1m

route:
  receiver: local-observer
  group_by:
    - alertname
    - service
    - severity
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: local-observer
